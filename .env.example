# ─── Saleor TMA Frontend — Environment Variables ─────────────────────────────
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# For Cloudflare Pages deployment, set these in the Pages dashboard:
#   Settings → Environment variables

# ─── Backend ──────────────────────────────────────────────────────────────────
# Base URL of the backend API (no trailing slash).
# All API calls will be prefixed with this URL.
# Example: https://api.yourdomain.com
VITE_BACKEND_BASE_URL=https://api.yourdomain.com

# ─── Development overrides ────────────────────────────────────────────────────
# Optional: Provide a mock initData string for local browser testing
# (bypasses Telegram WebApp requirement when testing outside Telegram)
# VITE_DEV_INIT_DATA=query_id=test&user=%7B%22id%22%3A0%2C%22first_name%22%3A%22Test%22%2C%22username%22%3A%22testuser%22%7D&auth_date=1700000000&hash=test
